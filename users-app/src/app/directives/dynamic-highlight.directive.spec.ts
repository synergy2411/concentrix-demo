import { DynamicHighlightDirective } from './dynamic-highlight.directive';

describe('DynamicHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
